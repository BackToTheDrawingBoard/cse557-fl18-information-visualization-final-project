/**
 * TopicNetwork contains the force-graph of the stories and topics.
 */
class TopicNetwork extends Window
{
	/**
	 * TopicNetwork extends Window
	 */
	constructor (parent_div)
	{
		super(parent_div);
		this.window.classed("topic-network-window", true);
	}
}
